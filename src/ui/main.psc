INCLUDE FILE ui.h

/*
 * @file main.psc
 * @brief Program entry point. Starts the UI state machine.
 *
 * The application is controlled by the UI state machine (ui_start).
 */

FUNCTION main()

    CALL ui_start()

END FUNCTION